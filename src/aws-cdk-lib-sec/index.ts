
import { SecStack } from './SecStack'
import { SecLambda } from './SecLambda'
import { Secure } from './Secure'
import * as cdk from 'aws-cdk-lib'

// cdk.aws_lambda.Function = SecLambda
// cdk.Stack = SecStack

export { cdk, SecStack, SecLambda, Secure }